export class CalcDays {
    constructor(firstDate, lastDate) {
        this.firstDate = firstDate
        this.lastDate = lastDate
        this.result = this.totalDaysResult()
    }

    totalDaysResult() {
        return 'hello'
    }
}